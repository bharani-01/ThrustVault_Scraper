# Motor Scraper — Dependencies
# Run: pip install -r requirements.txt

# ─── Web UI ───────────────────────────────────────────────────────────────
flask==3.1.0                 # Web server for the scraper UI + SSE streaming

# ─── AI / Groq ────────────────────────────────────────────────────────────
groq==0.9.0                  # Groq LLM API (llama3, mixtral) for smart parsing

# ─── Core HTTP + Bot-bypass ───────────────────────────────────────────────
curl_cffi==0.7.3             # Impersonates real browser TLS (JA3/JA4) → beats Cloudflare
cloudscraper==1.2.71         # Solves Cloudflare JS challenges (fallback)

# ─── Browser automation (JS-heavy sites) ─────────────────────────────────
playwright==1.44.0           # Real Chromium browser control
playwright-stealth==1.0.6    # Hides Playwright automation signals

# ─── HTML parsing ─────────────────────────────────────────────────────────
beautifulsoup4==4.12.3
lxml==5.2.2            # optional: install manually if you have build tools → pip install lxml


# ─── Data handling ────────────────────────────────────────────────────────
pandas==2.2.2

# ─── Resilience ───────────────────────────────────────────────────────────
tenacity==8.3.0              # Retry with exponential backoff

# ─── Utilities ────────────────────────────────────────────────────────────
python-dotenv==1.0.1         # Load .env inside motor_scraper/
rich==13.7.1                 # Beautiful terminal output + progress bars
