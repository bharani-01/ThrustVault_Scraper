[pytest]
markers =
    slow: marks tests as slow (network) — deselect with '-m "not slow"'

# Show summary of all non-passing tests
addopts = -ra

# Timeout per test (requires pytest-timeout)
# timeout = 120
